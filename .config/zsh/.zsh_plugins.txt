# 1. Load FPATH/Function extensions first
zsh-users/zsh-completions path:src kind:fpath
# Only keep P10k if you actually use it; otherwise, delete this line.
# romkatv/powerlevel10k kind:fpath 

# 2. Zephyr Core (High quality, keep these)
mattmc3/zephyr path:plugins/directory
mattmc3/zephyr path:plugins/editor
mattmc3/zephyr path:plugins/history
mattmc3/zephyr path:plugins/completion
mattmc3/zephyr path:plugins/color
mattmc3/zephyr path:plugins/macos

# 3. Prompt (Move up so it can start its async worker early)
spaceship-prompt/spaceship-prompt

# 4. Utilities & Aliases
peterhurford/up.zsh
mdumitru/git-aliases
$ZDOTDIR/.aliases

# 5. OMZ Plugins (Lightweight)
ohmyzsh/ohmyzsh path:lib/clipboard.zsh
ohmyzsh/ohmyzsh path:plugins/copyfile
ohmyzsh/ohmyzsh path:plugins/copypath
ohmyzsh/ohmyzsh path:plugins/extract
ohmyzsh/ohmyzsh path:plugins/magic-enter
ohmyzsh/ohmyzsh path:plugins/fancy-ctrl-z

# 6. Heavy Plugins (DEFER THESE)
# This ensures the prompt appears before these heavy scripts finish processing
zdharma-continuum/fast-syntax-highlighting kind:defer
zsh-users/zsh-autosuggestions kind:defer
zsh-users/zsh-history-substring-search kind:defer
